name: withcontracts

dependencies:
  - name: foodep
    local: ./dep

test:
  # `false` because running pytest within pytest.
  disconnect_providers_after: false

compile:
  exclude:
    - exclude_dir/*
    - Excl*.json
