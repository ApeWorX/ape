[flake8]
max-line-length = 100
exclude =
	venv*
	docs
	build
per-file-ignores =
    # Need signal handler before imports
    src/ape/__init__.py: E402
    # Seems like unused imports for other py-versions but it's not.
	src/ape/_compat.py: F401
